package enums;

/**
  * The class <code>WindowComponentState</code> is an enumeration to describe the differents states of the window
  * @version 1.0
  * @author Dorian Terbah 
**/

public enum WindowViewState {
    EDIT_MAP_STATE,
    CHOOSE_MAP_STATE,
    HOME_STATE,
    IN_GAME_STATE,
    PARAMETER_STATE
}